# Instructor materials — private companion doc, never publish
instructor_notes.*

# Raw data IS committed to this repo (see README) for full reproducibility.
# Only ignore the price-less 2025 variant and the calendar file.
listings_*.csv.gz
calendar.csv.gz

# R session cruft
.Rproj.user/
.Rhistory
.RData
.Ruserdata
_inspect.*
_shot.*
_check.*
